<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Fruits App</title>
    <link rel="stylesheet" href="/css/edit.css" />
  </head>
  <body>
    <section class="edit" id="edit">
      <h2 class="heading">Edit <span>Meat: <%= meat.name %> </span></h2>

      <form action="/meats/<%= id %>/?_method=PUT" method="POST">
        <div class="input-box">
          <label for="name">Name</label>
          <input type="text" name="name" id="name" value="<%= meat.name %>" />
        </div>
        <div class="input-box">
          <label for="quality">Quality</label>
          <input
            type="text"
            name="quality"
            id="quality"
            value="<%= meat.quality %>"
          />
        </div>
        <div class="input-box"></div>
        <label for="readyToEat">Is Ready to Eat?</label>
        <input type="checkbox" name="readyToEat" id="readyToEat" <%
        if(meat.readyToEat === true){ %> checked <% } %> />
        <textarea
          name=""
          id=""
          cols="30"
          rows="10"
          placeholder="Desciption"
        ></textarea>
        <input class="btn" type="submit" value="Edit Meat" />
      </form>
    </section>
  </body>
</html>
